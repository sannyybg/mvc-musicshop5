@model IEnumerable <muscshop.Models.Genre>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>Select from @Model.Count()</p>

<ul>
    @foreach (var genre in Model)
    {
        <li><a href="/Store/Browse?genre=@genre.Name">@genre.Name</a></li>
 
    }
</ul>
